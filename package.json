{
  "name": "bridge-os",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm -w @bridge/engine run build && npm -w @bridge/api run build",
    "test": "npm -w @bridge/api run test",
    "dev:api": "npm -w @bridge/api run dev"
  }
}
